// Stub de `server-only` pour les tests unitaires : le paquet original lève à
// l'import hors composant serveur Next.js ; vitest exécute les modules dans un
// environnement Node neutre, donc ce module vide suffit.
export default {};
